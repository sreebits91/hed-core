name: HED Performance Qualification

on:
  workflow_dispatch:
  push:
    branches: [feature/hed-v2-stability]

permissions:
  contents: read

env:
  GO_VERSION: '1.26.6'
  GOTOOLCHAIN: local
  GOFLAGS: '-mod=mod'

jobs:
  qualification:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - run: go mod download
      - name: Build load test
        run: go build -o /tmp/hed-loadtest ./cmd/loadtest
      - name: Measure real throughput tiers
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          : > artifacts/qualification.txt
          for tx in 100000 250000 500000 1000000 5000000; do
            echo "===== ${tx} transactions =====" | tee -a artifacts/qualification.txt
            timeout 15m /tmp/hed-loadtest -tx "$tx" -workers 256 | tee -a artifacts/qualification.txt
          done
          grep -Eq '^transactions=[0-9]+ .*workers=256 tps=[0-9]+(\.[0-9]+)?$' artifacts/qualification.txt
      - uses: actions/upload-artifact@v4
        with:
          name: hed-performance-qualification
          path: artifacts/qualification.txt

  profiles:
    needs: qualification
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - run: go mod download
      - name: Capture CPU, memory/allocation, mutex, block and goroutine profiles
        env:
          HED_GOROUTINE_PROFILE: ${{ github.workspace }}/profiling/goroutine.pprof
        run: |
          set -euo pipefail
          mkdir -p profiling
          go test ./pkg/benchmark -run '^TestProfilingCapture$' -mutexprofile=profiling/mutex.pprof -blockprofile=profiling/block.pprof
          test -s profiling/mutex.pprof
          test -s profiling/block.pprof
          test -s profiling/goroutine.pprof
          go test ./pkg/benchmark -run '^$' -bench='^BenchmarkRun256Workers$' -benchtime=3s -benchmem -cpuprofile=profiling/cpu.pprof -memprofile=profiling/memory.pprof | tee profiling/benchmark.txt
          test -s profiling/cpu.pprof
          test -s profiling/memory.pprof
      - name: Generate profile summaries
        run: |
          set -euo pipefail
          go tool pprof -top profiling/cpu.pprof | tee profiling/cpu-top.txt
          go tool pprof -top profiling/memory.pprof | tee profiling/memory-top.txt
          go tool pprof -top -alloc_objects profiling/memory.pprof | tee profiling/alloc-top.txt
          go tool pprof -top profiling/mutex.pprof | tee profiling/mutex-top.txt
          go tool pprof -top profiling/block.pprof | tee profiling/block-top.txt
          test -s profiling/alloc-top.txt
          test -s profiling/cpu-top.txt
          test -s profiling/mutex-top.txt
          test -s profiling/block-top.txt
      - uses: actions/upload-artifact@v4
        with:
          name: hed-all-profiles
          path: profiling/
          if-no-files-found: error
